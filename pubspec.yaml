name: rsi
description: "A new Flutter project."
publish_to: 'none'
version: 0.3.0+3

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  mapbox_maps_flutter: ^2.1.0
  latlong2: ^0.9.0
  http: ^1.2.0
  geolocator: ^10.1.0
  flutter_background_service: ^5.0.0
  flutter_local_notifications: ^19.4.0
  flutter_riverpod: ^2.5.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/naxos_hazards.geojson
    - assets/data/arta_hazards.geojson
    - assets/data/pyli_hazards.geojson
    - assets/data/rentals.json
    - assets/data/accidents.geojson
    - assets/data/road_segments.geojson
    - assets/legal/
    - assets/images/rentals/
